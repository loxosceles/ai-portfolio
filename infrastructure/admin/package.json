{
  "name": "admin-interface",
  "version": "0.2.6",
  "type": "module",
  "description": "Admin interface for portfolio data management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'Use pnpm test:infra from project root to run admin tests'",
    "test:watch": "echo 'Use pnpm test:infra --watch from project root'",
    "test:coverage": "echo 'Use pnpm test:infra --coverage from project root'"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.830.0",
    "@aws-sdk/lib-dynamodb": "^3.830.0",
    "@aws-sdk/client-ssm": "^3.830.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
